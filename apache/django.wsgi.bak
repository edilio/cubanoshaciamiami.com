import os
import sys

paths = ('C:/projects/',''C:/projects/cubanoshaciamiami/')
for path in paths:
    if path not in sys.path:
        sys.path.append(path)

os.environ['DJANGO_SETTINGS_MODULE'] = 'cubanoshaciamiami.settings'

import django.core.handlers.wsgi
application = django.core.handlers.wsgi.WSGIHandler()